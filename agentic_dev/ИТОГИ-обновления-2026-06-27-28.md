# Итоги обновлений «СПб Горы» — 27–28 июня 2026

**Репозиторий:** [spbmlaplus/SPb_Mountains](https://github.com/spbmlaplus/SPb_Mountains)  
**Ветка:** `main`  
**Production:** https://spbmlaplus.github.io/SPb_Mountains/  
**Последний коммит периода:** `89e123e` (28.06.2026, 11:02 МСК)

---

## Период

| День | Что произошло |
|------|----------------|
| **Сб 27.06** | Подготовка данных: экспорт слоёв и стилей из QGIS-проекта в `new_files/` (см. `agentic_dev/export-qgz-layers-2026-06-27.md`). Отдельного коммита в этот день нет — изменения вошли в коммит вс 28.06 `76c7523`. |
| **Вс 28.06** | Четыре коммита в `main`: фаза 5, mobile UX, фикс фото лонгрида, фикс фото на карте + ссылки MLA+. Автодеплой на GitHub Pages после каждого push. |

**Диапазон коммитов:** `76c7523` … `89e123e`  
**Сводка по diff:** 527 файлов, +23 632 / −19 667 строк (включая geojson, qml, фото, handoff-доки).

---

## Коммиты (хронология)

### 1. `76c7523` — Phase 5 polish: mask opacity, styles, layers, and longread updates

**28.06.2026, 10:02 МСК**

Крупное обновление «фазы 5»: карта, слои, лонгрид, клики, контент.

#### Карта и слои

- **Прозрачность масок** (`mask_*`, `mount_polygon`): fill-opacity **35%** / **50%** сохраняется после fade видимости слоёв; centroids (подписи) не затухают вместе с масками (`layerFade.ts`, `TargetOpacityResolver`).
- **Слой mount (▲):** без hover-fade, мгновенная видимость при смене секции.
- **Finns:** размер точек **×2** (id_map 12–13).
- **Подписи inscription (id_map 12–20):** компактный режим **10px**, смещение выше точки.
- **Стили из QGIS:** обновлены/добавлены QML → `layerStyles.ts`, `section-overlays.json`, `base-composition.json`.
- **Новые/обновлённые geojson** в `src/assets/layers/` и `new_files/layers/` (маски амфитеатра, landscape_*, historical_zones, walking_routes, mount, estate, id_map 23 и др.).
- **id_map 23:** маршруты, точки обзора (`23_1_points`), viewshed (`23_2_viewshed`), фото смотровых площадок.
- **Кликабельные объекты:** `clickTrigger.ts`, `mapHighlight.ts`, `objectPhotos.ts`, `MountainPopup` / модалки фото для гор и усадеб.
- **Горная живопись (id_map 21):** слой `21_живопись.geojson`, иконки `painting_images/*.webp`, клик по картине на карте.

#### Лонгрид и контент

- Обновлён **`fallbackLongread.ts`** (57+ пунктов) из `new_legend/`.
- Тип **«геологическая эра»** (`.longread-era`): полужирный курсив, отдельная opacity для текста vs медиа.
- Фото лонгрида в **`src/assets/photos/`** (Nazarov, amphitheater, landscape_*, Belveder и др.).
- Навигация: главы **06–11** убраны из sidebar намеренно; deep-link `#id_map=N` read-only.

#### Инфраструктура

- Скрипты: `copy-new-assets.py`, `gen-layers.py`, `gen-fallback-longread.py`, манифест `objectPhotoManifest.ts`.
- Handoff-документация фаз 2–5 в `agentic_dev/`.
- Тайлы: `VITE_TILE_BASE_URL` → `spbmlaplus.github.io/spb_mountains_tiles`.

---

### 2. `c6929bb` — Replace mobile carousel with draggable bottom sheet and vertical longread

**28.06.2026, 10:41 МСК**

Переработка **mobile UX** (только `@media (max-width: 768px)`; desktop не тронут).

| Было | Стало |
|------|--------|
| Split 55% карта / 45% горизонтальная карусель | Карта на весь экран, bottom sheet поверх |
| `MobileLongreadControls` (стрелки, dots) | `MobileLongreadSheet.tsx` (Vaul) |

**Поведение sheet:**

- Snap: **96px** (свёрнуто: ручка + pill) и **45%** (развёрнуто).
- Вертикальный scroll лонгрида (как на desktop).
- Fade текста под pill (`.mobile-longread-sheet__body::before`).
- Drag только за ручку (`handleOnly`).
- `modal={false}` — карта кликабельна при свёрнутом sheet.
- Пункт меню → `requestMobileSheetExpanded()` + scroll к секции.

**Файлы:** `MobileLongreadSheet.tsx`, `App.css`, `MapInteractionContext.tsx`, `MainMap.tsx`. Удалён `MobileLongreadControls.tsx`.

---

### 3. `6f06fc4` — Fix longread photos when content loads from Google Sheets

**28.06.2026, 10:50 МСК**

**Баг:** с фазы 5 UI читает `item.media`, а парсер Google Sheets заполнял только `mediaLink` → фото в лонгриде пропадали после загрузки таблицы (desktop + mobile).

**Фикс в `MainMap.tsx`:**

- `buildMediaFromLink()` — строит `{ link, side, caption? }` из `mediaLink`.
- `normalizeItemMedia()` — в `parseSheetRows` и `fillForwardIdMap`.

Fallback (`fallbackLongread.ts`) уже содержал `media`; Sheets теперь тоже.

---

### 4. `89e123e` — Deploy click-trigger photos and add MLA+ links

**28.06.2026, 11:02 МСК**

#### Фото на карте (попапы гор, живопись, смотровые)

**Баг:** `objectPhotoManifest.ts` знал о файлах (`new_files/mount/`, `new_files/21/`, `new_files/1/`), но `public/object_photos/` в `.gitignore` и **не копировался при CI** → на GitHub Pages **404** (битые превью в попапе «Вершина кама», иконки живописи и т.д.).

**Фикс:**

- `python scripts/copy-new-assets.py` перед сборкой в **`package.json`** (`npm run build`) и **`.github/workflows/deploy.yml`**.
- Источник фото в git: `new_files/mount/` (43 файла), `new_files/21/` (40), `new_files/1/` (64).

#### Горная живопись — просмотр фото

- Клик по иконке картины: **модалка** с подписью (как у гор), затем fullscreen по клику на изображение (раньше сразу fullscreen).

#### Внешние ссылки

| Элемент | URL |
|---------|-----|
| **О проекте** (sidebar + mobile menu) | https://mlaplus.ru/projects/laboratories/gornyj-peterburg |
| **МЛА+** (свёрнутый sidebar, mobile top bar, mobile menu) | https://mlaplus.ru/ |

Файл констант: `src/externalLinks.ts`. Стили ссылок: `layout.css`.

---

## Что проверить на production

| Проверка | Как |
|----------|-----|
| Сайт открывается | https://spbmlaplus.github.io/SPb_Mountains/ |
| Фото лонгрида | Nazarov, amphitheater, landscape_* — desktop и mobile |
| Попап горы с фото | Клик по ▲ «гора» → «Вершина кама» и др. — 2 превью |
| Горная живопись | `#id_map=21` → иконки на карте, клик → модалка |
| Mobile sheet | 375px: drag вниз → свёрнуто; scroll текста; fade под pill |
| Ссылки | «О проекте», «МЛА+» → mlaplus.ru |
| Маски амфитеатра | `#id_map=3` → fill-opacity ~35% |
| Смотровые + маршруты | `#id_map=23` |

После деплоя — **Ctrl+F5**, если кэш старой сборки.

---

## Ключевые файлы (шпаргалка)

| Назначение | Путь |
|------------|------|
| Карта, лонгрид, парсер Sheets | `src/MainMap.tsx` |
| Mobile bottom sheet | `src/MobileLongreadSheet.tsx` |
| Fade слоёв | `src/layerFade.ts` |
| Стили слоёв | `src/layerStyles.ts` |
| Оверлеи id_map | `src/sectionOverlays.ts`, `src/assets/sections/section-overlays.json` |
| Клики / попапы | `src/clickTrigger.ts`, `src/MountainPopup.tsx` |
| Фото кликов | `src/objectPhotos.ts`, `public/object_photos/` (генерируется при build) |
| Фото лонгрида | `src/assets/photos/` |
| Fallback контент | `src/fallbackLongread.ts` |
| Деплой | `.github/workflows/deploy.yml` |
| Внешние ссылки | `src/externalLinks.ts` |

---

## Локальная разработка

```powershell
cd C:\Work\SPb_Mountains\SPb_Mountains
$env:VITE_TILE_BASE_URL = "https://spbmlaplus.github.io/spb_mountains_tiles"
$env:VITE_BASE_PATH = "/SPb_Mountains/"
npm run dev
```

`npm run build` автоматически копирует `object_photos` из `new_files/`.

---

## Не вошло в коммиты (локально, по состоянию на конец 28.06)

- `agentic_dev/HANDOFF-current-state.md` — черновик handoff (устаревшее описание carousel).
- Правки `agentic_dev/HANDOFF-phase5-layer-opacity.md` — не запушены.
- `new_legend/~$longread (2).xlsx` — временный файл Excel (не коммитить).

---

## История коммитов (кратко)

```
76c7523  Phase 5 polish: mask opacity, styles, layers, and longread updates.
c6929bb  Replace mobile carousel with draggable bottom sheet and vertical longread.
6f06fc4  Fix longread photos when content loads from Google Sheets.
89e123e  Deploy click-trigger photos and add MLA+ links.          ← HEAD main
```

**Итоговая ссылка для заказчика:** https://spbmlaplus.github.io/SPb_Mountains/
